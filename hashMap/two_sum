// Leetcode Problem:
// brute force approach:-
// approach:
// use a nested loop where the outer loop considers each element and the inner loop sums each
// value of the outer loop to check if the sum equals the target.
// once the sum equals the target, return those two indices; otherwise, return -1 if no such pair is found.
// TC: O(N^2), due to the use of two nested loops.
// SC: O(1), as there is no extra space used.

var twoSum = function (arr, target) {
    for (let i = 0; i < arr.length - 1; i++) {
        for (let j = i + 1; j < arr.length; j++) {
            if (arr[i] + arr[j] === target) {
                return [i, j];
            }
        }
    }

    return -1;
}

// Optimal approach:-
// approach:
// use of a map (hash table) to store the indices of the numbers we have already seen.
// for each element in the array, i will calculate the required number to reach the target (requiredNumber = target - nums[i]).
// Check if this required number is already in the map:
// - If it is, return the indices of the required number and the current element (nums[i]).
// - If it isn't, store the current element (nums[i]) and its index in the map.
// This ensures that we only traverse the array once, providing an efficient solution.
// TC: O(N), as we traverse the array once and perform constant-time operations for each element.
// SC: O(N), due to the extra space used by the map to store the elements and their indices.

var twoSum = function(nums, target){
    let map = new Map();
    for(let i = 0; i < nums.length; i++){
        let requiredNumber = target - nums[i];
        if(map.has(requiredNumber)){
            return [map.get(requiredNumber), i];
        }else{
            map.set(nums[i], i);
        }
    }
}
