// Leetcode Problem: Group Anagrams
// Approach:
// - Use of hash map to store groups of anagrams together.
// - Use of an array to collect all the groups of anagrams.
// - While traversing each string, first sort the string and convert it back into a string,
//   so that it will be easy to group the anagrams together.
// - Then check if the sorted string already exists as a key in the map:
//     - If it does not exist, store the sorted string as a key in the map,
//       and set its value as an array containing the original string.
//     - If it already exists, push the original string into the array
//       corresponding to the sorted string in the map.
// - Once all groups of anagrams are found, traverse the map and push its values
//   (which contain groups of anagrams) into the result array.
//
// Time Complexity: O(N * K log K)
// - O(N) to traverse the list of strings.
// - O(K log K) to sort each string (K = average length of a string).
// - Overall: O(N * K log K)
//
// Space Complexity: O(NK)
// - O(N) for the map to store groups of anagrams.
// - O(N) for the result array to store all groups of anagrams.
// - O(K) per string for the temporary character array created by split().
// - Overall: O(NK)



var groupAnagrams = function (strs) {
    let map = new Map();
    for (let i = 0; i < strs.length; i++) {
        let word = strs[i].split("").sort().join("");
        if (map.has(word)) {
            map.get(word).push(strs[i])
        } else {
            map.set(word, [strs[i]])
        }
    }

    let anagram = [];
    for(let val of map.values()){
        anagram.push(val)
    }

    return anagram;
}