// Leetcode Problem:
// brute force approach:
// approach:
// first convert both the strings into arrays so that we can easily traverse through them.
// traverse the ransomNote array and check if the current character exists in the magazine array.
// if it exists, fetch the index of that character from the magazine array.
// remove that character from the magazine array so that we can check each character of ransomNote.
// if all characters of ransomNote exist in the magazine, return true. Otherwise, return false.
// TC: O(N^2), Explanation:
// O(N) to convert both ransomNote and magazine strings into arrays using split() method.
// O(N^2) to traverse through the ransomNote array and check each character of ransomNote in the magazine array using 
// indexOf() and remove it using splice().
// overall TC: O(N) + O(N^2) = O(N^2)
// SC: O(M + N) as we split the ransomNote and magazine strings into arrays, creating new arrays.

var canConstruct = function (ransomNote, magazine) {
    ransomNote = ransomNote.split('');
    magazine = magazine.split('');
    for (let i = 0; i < ransomNote.length; i++) {
        let index = magazine.indexOf(ransomNote[i]);
        if (index === -1) {
            return false;
        } else {
            magazine.splice(index, 1);
        }
    }
    return true;
};

// optimal approach:
// approach:
// use a hash map to store the count of each character in the magazine string.
// traverse the ransomNote string to check if each character exists in the Map.
// if it exists, decrement the character's count in the Map.
// if it does not exist or the character's count is zero, return false immediately since, ransomeNote cannot be constructed
// with characters of magazine.
// if all characters of ransomNote exist in the magazine, return true.
// TC: O(N), Explanation:
// O(N) to traverse the magazine string and store each character's count in the Map.
// O(M) to check each character of ransomNote in the Map.
// overall TC: O(N) + O(M) = O(N)
// SC: O(N), as the Map is used to store all characters of the magazine.

var canConstruct = function (ransomNote, magazine) {
    let map = new Map();
    for (let i = 0; i < magazine.length; i++) {
        if (map.has(magazine[i])) {
            map.set(magazine[i], map.get(magazine[i]) + 1);
        } else {
            map.set(magazine[i], 1);
        }
    }

    for (let i = 0; i < ransomNote.length; i++) {
        if (!map.has(ransomNote[i]) || map.get(ransomNote[i]) === 0) {
            return false;
        } else {
            map.set(ransomNote[i], map.get(ransomNote[i]) - 1);
        }
    }

    return true;
}
