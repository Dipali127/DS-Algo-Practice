// Why is QuickSort preferred over MergeSort?
// QuickSort is preferred over MergeSort because it is an in-place algorithm and uses less memory. 
// Its average and best-case time complexity is O(N log N), and its space complexity is O(log N). 
// On the other hand, MergeSort requires an extra array, so its space complexity is always O(N).