// leetcode Problem:
// Fibonacci sequence: 0, 1, 1, 2, 3, 5, 8, 13, ...
// nth Fibonacci number is calculated by adding the two preceding terms.
// Approach: Recursively call the fib function twice to find the nth Fibonacci number,
// and stop the recursion once the base case is reached.
// TC: O(2^n) as for getting the nth Fibonacci number, it recursively calls the fib function twice.
// SC: O(n), which is the maximum depth of the recursion stack.

var fib = function(n) {
    if(n==0 || n==1){
        return n;
    }

    return fib(n-2)+fib(n-1)
};


// iterative appraoch:-
// explanation:- 
//  If n is 0 or 1, return n directly.
// Initialize prev1 to 0 and prev2 to 1. These variables represent the previous two Fibonacci numbers.
//  Iterate from 2 to n, updating the current Fibonacci number as the sum of the previous two (prev1 and prev2).
// Update prev1 and prev2 accordingly and return the current fibonacci number.
// TC:O(N), since the loop runs from 2 to n and SC:O(1)
var fib = function(n) {
    if (n === 0) {
        return 0;
    }
    if (n === 1) {
        return 1;
    }

    let prev1 = 0, prev2 = 1;
    let current;
    for (let i = 2; i <= n; i++) {
        current = prev1 + prev2;
        prev1 = prev2;
        prev2 = current;
    }

    return current;
}