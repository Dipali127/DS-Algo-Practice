// Leetcode Problem:
 // appraoch:- first find largest and smallest number from the array 'nums' and then call the gcd
 // function to get the highest common divisor (HCF) between largest and smallest number.
 // for finding gcd, use "EUCLIDEAN ALGORITHM":- the algorithm says that gcd of two number doesn't
 // change if the smaller number is subtracted from largest number.
 // "Euclidean algorithm" is the process of repeated subtration and carrying the result forward each
 // time until the result is equal to any one number being subtracted.
 // TC: O(N), explanation:- O(N) to iterate through the nums array to find largest and smallest number
 // and O(log(min(a,b))). So, overall TC:O(N+LOG(min(a,b))) and SC:O(log(min(a,b))), which is the maximum 
 // depth of the recursion stack.
 
var findGCD = function(nums) {
    let largest = Number.MIN_SAFE_INTEGER;
    let smallest = Number.MAX_SAFE_INTEGER
    for(let i=0;i<nums.length;i++){
        largest = Math.max(largest,nums[i]);
        smallest = Math.min(smallest,nums[i]);
    }

    let greatestNum = gcd(largest,smallest);
    function gcd(a,b){
        if(a === 0){
            return b
        }
        if( b === 0){
            return a;
        }

        if(a === b){
            return a;
        }

        if(a>b){
            return gcd(a-b,b);
        }else{
            return gcd(a,b-a);
        }
    }

    return greatestNum
};