// Leetcode Problem:
// Problem:- we have given a matrix such that if a matrix cell matrix[i][j] is 0, 
// then all the cells in its ith row and jth column will become 0.
// Optimal Approach:
// approach:
// i will use two arrays, `arrRow` and `arrCol`, to track which rows and columns contain a zero.
// initially, all elements in `arrRow` and `arrCol` are set to 1.
// first, i will iterate through the given matrix to identify which rows and columns contain a zero.
// Whenever I found a zero while iterating, I will update the corresponding row in arrRow and the corresponding column in arrCol to zero.
// after then i will iterate through `arrRow` to check which rows contain a zero.
// once i found a zero in arrRow, i will set all the elements in that row to zero.
// likewise, i will iterate through `arrCol` to check which columns contain a zero.
// once i found a zero in arrCol, i will set all the elements in that column to zero.

// Time Complexity: O(N^2)
// - O(N^2) due to the first nested loop that traverses the given matrix to mark the rows and columns containing zeroes.
// - O(N^2) due to the second nested loop that updates the matrix based on the marked rows (in the worst case, all rows might be updated).
// O(N^2) due to the third nested loop that updates the matrix based on the marked columns (in the worst case, all columns might be updated).
// - Overall TC: O(N^2).

// Space Complexity: O(N + M)
// - O(N) for storing row indices with zero values.
// - O(M) for storing column indices with zero values.
// - Overall SC: O(N + M).
var setZeroes = function(matrix) {
    let m = matrix.length;
    let n = matrix[0].length;
    let arrRow = new Array(m).fill(1);
    let arrCol = new Array(n).fill(1);

    // Step 1: Identify rows and columns that contain zeroes
    for (let i = 0; i < m; i++) {
        for (let j = 0; j < n; j++) {
            if (matrix[i][j] === 0) {
                arrRow[i] = 0;
                arrCol[j] = 0;
            }
        }
    }

    // Step 2: Set entire row to zero if it was marked
    for (let i = 0; i < m; i++) {
        if (arrRow[i] === 0) {
            for (let j = 0; j < n; j++) {
                matrix[i][j] = 0;
            }
        }
    }

    // Step 3: Set entire column to zero if it was marked
    for (let j = 0; j < n; j++) {
        if (arrCol[j] === 0) {
            for (let i = 0; i < m; i++) {
                matrix[i][j] = 0;
            }
        }
    }
};

