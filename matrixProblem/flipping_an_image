// LeetCode Problem:
// Approach: 
// First, iterate through the matrix to flip the image horizontally. 
// This means reversing each row (swap elements before or at the middle of the row 
// with the elements after the middle).
// Once each row is reversed, iterate through the matrix again to invert the image, 
// i.e., change 0s to 1s and 1s to 0s.

// Time Complexity: O(N^2)
// - O(N * N/2) for flipping each row horizontally
// - O(N^2) for inverting the matrix
// Overall: O(N^2)
// Space Complexity: O(1), since no additional space is used.


var flipAndInvertImage = function(image) {
    let m = image.length;
    let n = image[0].length;
    for(let i = 0; i < image.length; i++){
        for(let j = 0; j < Math.floor(n/2); j++){
            let temp = image[i][j];
            image[i][j] = image[i][n-j-1];
            image[i][n-j-1] = temp;
        }
    }

    for(let i = 0; i < m; i++){
        for(let j = 0; j < n; j++){
            if(image[i][j ]=== 1){
                image[i][j] = 0;
            }else{
                image[i][j] = 1;
            }
        }
    }

    return image;
};
